Copyright (c) Abstract Machines
# SPDX-License-Identifier: Apache-2.0

{{- if .Values.influxdb.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-influxdb-reader
spec:
  selector:
    app: {{ .Release.Name }}
    component: influxdb-reader
  ports:
    - port: {{ .Values.influxdb.reader.httpPort }}
      protocol: TCP
      name: {{ .Release.Name }}-influxdb-writer-{{ .Values.influxdb.reader.httpPort }}
{{- end }}
