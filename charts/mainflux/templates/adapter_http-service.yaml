apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-adapter-http
spec:
  selector:
    app: {{ .Release.Name }}
    component: adapter-http
  ports:
    - port: 8185
      protocol: TCP
