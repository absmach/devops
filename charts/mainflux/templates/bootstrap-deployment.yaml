# Copyright (c) Abstract Machines
# SPDX-License-Identifier: Apache-2.0

{{- if .Values.bootstrap.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-bootstrap
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}
      component: bootstrap
  template:
    metadata:
      annotations:
        prometheus.io/path: /metrics
        prometheus.io/port: "{{ .Values.bootstrap.httpPort }}"
        prometheus.io/scrape: "true"
      labels:
        app: {{ .Release.Name }}
        component: bootstrap
    spec:
      containers:
        - env:
            - name: MG_JAEGER_URL
              value: "http://{{ .Release.Name }}-jaeger-collector:{{ .Values.defaults.jaegerCollectorPort }}/api/traces"
            - name: MG_THINGS_URL
              value: http://{{ .Release.Name }}-things:{{ .Values.things.httpPort }}
            - name: MG_BOOTSTRAP_DB
              value: {{ index .Values  "postgresqlbootstrap" "auth" "database" }}
            - name: MG_BOOTSTRAP_DB_HOST
            {{- if .Values.postgresqlbootstrap.enabled }}
              value: {{ .Release.Name }}-postgresqlbootstrap
            {{- else }}
              value: {{ .Values.postgresqlbootstrap.postgresqlHost | quote }}
            {{- end }}
            - name: MG_BOOTSTRAP_DB_PASS
              value: {{ index .Values  "postgresqlbootstrap" "auth" "password" }}
            - name: MG_BOOTSTRAP_DB_PORT
              value: {{ .Values.postgresqlbootstrap.postgresqlPort | quote }}
            - name: MG_BOOTSTRAP_DB_USER
              value: {{ index .Values  "postgresqlbootstrap" "auth" "username" }}
            - name: MG_THINGS_ES_URL
              value: {{ .Release.Name }}-redis-streams-master:{{ .Values.things.redisESPort }}
            - name: MG_BOOTSTRAP_ES_URL
              value: {{ .Release.Name }}-redis-streams-master:{{ .Values.bootstrap.redisESPort }}
            - name: MG_BOOTSTRAP_PORT
              value: "{{ .Values.bootstrap.httpPort }}"
            - name: MG_BOOTSTRAP_LOG_LEVEL
              value: {{ default .Values.defaults.logLevel .Values.bootstrap.logLevel }}
            - name: MG_BOOTSTRAP_DB_SSL_MODE
              value: disable
            - name: MG_AUTH_GRPC_URL
              value: {{ .Release.Name }}-envoy:{{ .Values.users.grpcPort }}
            - name: MG_SDK_BASE_URL
              value: http://{{ .Release.Name }}-things:{{ .Values.things.httpPort }}
          image: "{{ default .Values.defaults.image.repository .Values.bootstrap.image.repository }}/bootstrap:{{ default .Values.defaults.image.tag .Values.bootstrap.image.tag }}"
          imagePullPolicy: {{ default .Values.defaults.image.pullPolicy .Values.bootstrap.image.pullPolicy }}
          name: {{ .Release.Name }}-bootstrap
          ports:
            - containerPort: {{ .Values.bootstrap.httpPort }}
              protocol: TCP
      {{- with .Values.defaults.image.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 12 }}
      {{- end }}
      dnsPolicy: ClusterFirst
      restartPolicy: Always
{{- end }}
